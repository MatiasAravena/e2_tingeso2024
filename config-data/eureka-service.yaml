server:
  port: 8080
eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
    server-url:
      default-zone: http://localhost:8080/eureka/
    instance:
      hostname: localhost